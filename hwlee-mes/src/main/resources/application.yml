spring:
  application:
    name: hwlee-mes

  datasource:
    url: jdbc:mysql://localhost:3308/mes_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: mes
    password: mes
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: false
    locations: classpath:db/migration

  # Phase 14 — MES→ERP 실적 전송용 Kafka (실제 토픽/리스너는 후속 Phase)
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: mes
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

server:
  # 8081 은 React Native Metro 기본 포트라 충돌 → 8082 사용.
  port: 8082

# 생산 시뮬레이터 — 설비 가동(RUNNING) + 작업지시 진행중(IN_PROGRESS)이면 자동 생산(학습용 데모).
mes:
  simulator:
    enabled: true
    tick-ms: 2000            # 2초마다 한 틱
    rate: 0.12               # 틱당 지시수량의 12% 생산 (약 8~9틱 = 15~20초에 완료)
    defect-probability: 0.15 # 틱당 15% 확률로 불량 1개

# 분산 추적 — 모든 요청 샘플링(학습), Zipkin 으로 전송
management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

logging:
  level:
    org.hibernate.SQL: DEBUG
