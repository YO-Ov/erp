# ─────────────────────────────────────────────────────────────
# 포트폴리오 정적 서버 (컨테이너 내부 :80)
#   바깥 메인 Caddy(hyunwoo.pro)가 전부 이 컨테이너로 프록시한다.
#   단일 페이지라 SPA 폴백만 두면 어떤 경로든 index.html 로 응답.
#   내부 통신이므로 TLS 불필요(HTTP :80). 자동 HTTPS 비활성.
# ─────────────────────────────────────────────────────────────
{
	auto_https off
	admin off
}

:80 {
	root * /srv
	encode gzip zstd
	try_files {path} /index.html
	file_server
}
