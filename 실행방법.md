# 🚀 실행 / 종료 — 한눈에

> 시스템이 2개(ERP·MES) + 도커(DB 2개, Kafka, Zipkin)라 순서가 있다. 아래 순서대로만 하면 된다.

## ① 실행 (순서대로)

```bash
# 1) 도커 인프라 (DB×2, Kafka, Zipkin) — 맨 처음 한 번
cd /Users/ddakple/IdeaProjects/my-app/erp
docker compose up -d

# docker DB 사용 시
# 2) ERP 실행  (터미널 1)
cd hwlee-erp && SPRING_PROFILES_ACTIVE=local ./gradlew bootRun

# 3) MES 실행  (터미널 2 = 새 탭)
cd hwlee-mes && SPRING_PROFILES_ACTIVE=local ./gradlew bootRun

# aws DB 사용 시
cd hwlee-erp && SPRING_PROFILES_ACTIVE=aws ./gradlew bootRun
cd hwlee-mes && SPRING_PROFILES_ACTIVE=aws ./gradlew bootRun
```

## ② 종료

```bash
# 1) ERP·MES 앱: 각 터미널에서  Ctrl + C
# 2) 도커 멈추기 (데이터는 보존됨)
cd /Users/ddakple/IdeaProjects/my-app/erp
docker compose stop
```

## ③ 접속 주소

| 무엇 | 주소 | 로그인 |
| --- | --- | --- |
| **ERP 대시보드/화면** | http://localhost:8080 | `admin@hyunwoo.com` / `pass1234` |
| **MES 현장 화면** | http://localhost:8082/work-orders | (로그인 없음) |
| Zipkin (분산 추적) | http://localhost:9411 | — |
| ERP Swagger (API 문서) | http://localhost:8080/swagger-ui.html | 위 계정 |

---
---

# 📚 더 알아두면 좋은 것 (필요할 때만)

## 1. 로그인 계정 (ERP)

비밀번호는 모두 `pass1234`. 역할별로 보이는 메뉴가 다르다.

| 계정 | 역할 | 용도 |
| --- | --- | --- |
| `admin@hyunwoo.com` | ADMIN | 전부 다 보임 (기본 추천) |
| `kim@hyunwoo.com` | SALES | 영업(SD) |
| `lee@hyunwoo.com` | FINANCE | 회계(FI)·리포트 |
| `park@hyunwoo.com` | PRODUCTION | 생산(PP)·MES 전송 |
| `jung@hyunwoo.com` | HR | 인사·급여 |

## 2. 잘 떴는지 확인하는 법

```bash
# ERP 살아있나
curl http://localhost:8080/actuator/health
# MES 살아있나
curl http://localhost:8082/api/ping
# 도커 컨테이너 상태 (전부 healthy 여야 정상)
docker compose ps
```

## 3. 포트 / DB 정보

| 항목 | 포트 | 메모 |
| --- | --- | --- |
| ERP 앱 | 8080 | |
| MES 앱 | 8082 | (8081은 다른 프로젝트가 써서 8082로 변경함) |
| ERP DB (MySQL) | 3307 | DB이름 `erp_db`, 계정 `erp`/`erp` |
| MES DB (MySQL) | 3308 | DB이름 `mes_db`, 계정 `mes`/`mes` |
| Kafka | 9092 | ERP↔MES 비동기 메시지 |
| Zipkin | 9411 | 분산 추적 화면 |

## 4. 자주 겪는 문제

### "Port 8080 (또는 8082) was already in use" — 포트가 이미 쓰임
이전에 띄운 앱이 안 꺼졌을 때. 그 포트를 쓰는 프로세스를 찾아 종료:
```bash
lsof -ti tcp:8080 -sTCP:LISTEN | xargs kill   # ERP
lsof -ti tcp:8082 -sTCP:LISTEN | xargs kill   # MES
```

### 앱은 떴는데 DB 연결 오류
도커가 안 떠 있거나 아직 준비 안 됨. 상태 확인 후 잠깐 기다렸다 재시도:
```bash
docker compose ps          # mysql 들이 healthy 인지
docker compose up -d        # 안 떠 있으면 다시
```

### MES 실적이 ERP에 안 들어옴 (Kafka)
Kafka 컨테이너가 떠 있어야 한다. `docker compose ps` 로 `hwlee-kafka` 가 healthy 인지 확인.

## 5. 완전 종료 / 초기화 (주의)

```bash
docker compose down          # 컨테이너 삭제 (데이터 볼륨은 남음)
docker compose down -v       # ⚠️ 데이터까지 전부 삭제 (DB 초기화됨)
```

---

> 이 파일은 "빠른 실행" 용. 학습 자료는 `doc/`, 진행 현황은 `PROGRESS.md` 참고.
